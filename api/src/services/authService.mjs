// import oauthService from "../services/oauthService.mjs";

// const authService = {
//   registerUser: async function (email, password, username) {
//     if (!email || !password) {
//       return res.status(400).json({
//         message: "Mssing email or password",
//       });
//     }

//     const user = await authModel.registerUser(email, password, username);

//     return user;
//   },
//   loginUser: async function (email, password, username) {
//     if (!email || !password) {
//       return res.status(400).json({
//         message: "Mssing email or password",
//       });
//     }

//     const token = await authModel.loginUser(email, password, username);
//   },
// };

// export default authService;
